---
import path from 'path';
const { src, ...rest } = Astro.props;
const image = path.join(import.meta.env.BASE_URL, 'images', src);
---

<img src={ image } { ...rest } />

<style>
  img {
    max-width: 100%;
  }
</style>